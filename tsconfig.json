{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": {
      "@*": [
        //Diretório relativo para o src
        "./src*" //colocar o src no ./ porque o expo cria o projeto sem pasta source
      ]
    }
  },
  "include": ["**/*.ts", "**/*.tsx", ".expo/types/**/*.ts", "expo-env.d.ts"]
}
